import styled from 'styled-components';

const footerStyles = styled.div`
  .card:hover {
    cursor: pointer;
  }

  .brand-image {
    height: 50px; 
    width: 100px;
  }
`;

export default footerStyles;
